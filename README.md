# WorkflowTaskManager
ulti-role task management system with approval workflow (Admin/Approver/Publisher/User). Features JWT auth, email notifications, file uploads, comments, due date reminders via Hangfire, and PostgreSQL.
